{
  "name": "antigravity-grid-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "pnpm --filter server start",
    "build": "pnpm --filter client build",
    "dev": "pnpm -r --parallel dev"
  }
}
